// src/types/express.d.ts
import { JwtPayload } from 'jsonwebtoken';

declare module 'express' {
  interface Request {
    user?: JwtPayload & { id: number; role: string }; // Расширяем Request, добавляя user
  }
}